import { Layout, Playground, Attributes } from 'lib/components'
import { Tabs, Spacer, Link, Text, Button, Code } from 'components'
import { useState } from 'react'
import TwitterIcon from '@zeit-ui/react-icons/twitter'
import TwitchIcon from '@zeit-ui/react-icons/twitch'
import removeDeps from 'lib/removeDeps.js'

import defaultTabs from '!!raw-loader!pages/code/tabs/default.jsx'
import solidTabs from '!!raw-loader!pages/code/tabs/solid.jsx'
import controlledTabs from '!!raw-loader!pages/code/tabs/controlled.jsx'
import deviderTabs from '!!raw-loader!pages/code/tabs/devider.jsx'
import customBottom from '!!raw-loader!pages/code/tabs/customBorder.jsx'
import dynamicContent from '!!raw-loader!pages/code/tabs/dynamicContent.jsx'
import imperative from '!!raw-loader!pages/code/tabs/imperative.jsx'
import onChange from '!!raw-loader!pages/code/tabs/onChange.jsx'

export const meta = {
  title: 'tabs',
  group: 'Navigation',
}

## Tabs

Display tab content.

<Playground
  title="default"
  scope={{ Tabs }}
  desc={`Toggle display of different tabs. without caring about the component state. 
  This should be the most common use case
  `}
  code={removeDeps(defaultTabs)}
/>
<Playground
  title="Solid"
  scope={{ Tabs }}
  desc={`This is a solid varient`}
  code={removeDeps(solidTabs)}
/>
<Playground
  title="Controlled"
  scope={{ Tabs }}
  desc={`The component become controlled when value present`}
  code={removeDeps(controlledTabs)}
/>

<Playground
  scope={{ Tabs }}
  title="Custom Bottom Border"
  desc={`Users can difine their own bottom border`}
  code={removeDeps(customBottom)}
/>
<Playground
  scope={{ Tabs }}
  title="Devider"
  desc={`Choose to show devider`}
  code={removeDeps(deviderTabs)}
/>

<Playground
  title="Dynamic Tab content"
  scope={{ Tabs, Text, Spacer, Button }}
  desc={`Tab content can be controlled by user`}
  code={`${removeDeps(dynamicContent)}`}
/>
<Playground
  title="Get/Set Active Tab"
  scope={{ Tabs, Button, Text, Spacer }}
  desc={`The Tab ref expose an interface to set and get the state imperatively`}
  code={removeDeps(imperative)}
/>

<Playground
  title="Listen to the change from the component "
  scope={{ Tabs, Text, Spacer }}
  desc={`The compoent also exposed an onChange props to monitor the tab change`}
  code={removeDeps(onChange)}
/>

<Attributes edit="/pages/en-us/components/tabs.mdx">
<Attributes.Title>Tabs.Props</Attributes.Title>

| Attribute        | Description                   | Type                    | Accepted values                 | Default |
| ---------------- | ----------------------------- | ----------------------- | ------------------------------- | ------- |
| **initialValue** | initial value                 | `string`                | -                               | -       |
| **value**        | controlled when value present | `string`                | -                               | -       |
| **onChange**     | change event                  | `(val: string) => void` | -                               | -       |
| **showDivider**  | if devider present            | `boolean`               | `true` or `false`               | -       |
| ...              | native props                  | `HTMLAttributes`        | `'alt', 'id', 'className', ...` | -       |

<Attributes.Title>Tabs.ref</Attributes.Title>

| Attribute      | Description                                             | Accepted values                         | Default |
| -------------- | ------------------------------------------------------- | --------------------------------------- | ------- |
| **currentTab** | act as both a getter and setter regarding to active tab | `string`(setter) or `undefined`(getter) | -       |

<Attributes.Title alias="Tabs.Tab">Tabs.Item.Props</Attributes.Title>

| Attribute            | Description         | Type                          | Accepted values | Default |
| -------------------- | ------------------- | ----------------------------- | --------------- | ------- |
| **label**            | display tab's label | `string` or `React.ReactNode` | -               | -       |
| **value** (required) | unique ident value  | `string`                      | -               | -       |
| **disabled**         | disable current tab | `boolean`                     | -               | `false` |

</Attributes>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
